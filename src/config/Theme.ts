import { createTheme } from "@mui/material/styles";

const ThemeConfig = createTheme({
  palette: {
    primary: {
      light: "#FEAFA0",
      main: "#FF8F8B",
      dark: "#FF6E75",
    },
  },
});

export default ThemeConfig;
